import { UnlessStructDirective } from './unless-struct.directive';

describe('UnlessStructDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlessStructDirective();
    expect(directive).toBeTruthy();
  });
});
