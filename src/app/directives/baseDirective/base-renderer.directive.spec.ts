import { BaseRendererDirective } from './base-renderer.directive';

describe('BaseRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseRendererDirective();
    expect(directive).toBeTruthy();
  });
});
